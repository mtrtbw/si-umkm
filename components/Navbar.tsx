// components/Navbar.tsx
/*import Image from 'next/image';
import Link from 'next/link';

export default function Navbar() {
  return (
    <nav className="w-full max-w-6xl mx-auto px-6 py-4 mt-24 flex items-center justify-between bg-black bg-opacity-50 rounded-xl shadow-lg">
      {/* Logo }
      <div className="flex items-center gap-3">
        <Image
          src="/logo.png"
          alt="Logo Si-UMKM"
          width={48}
          height={48}
          className="object-contain"
        />
      </div>

      {/* Navigasi }
      <div className="flex items-center gap-4">
        <Link href="/produk">
          <button className="bg-yellow-400 hover:bg-yellow-500 text-black font-semibold px-5 py-2 rounded-xl transition">
            Lihat Produk
          </button>
        </Link>
        <Link href="/login">
          <button className="border border-yellow-400 hover:bg-yellow-400 hover:text-black text-white font-semibold px-5 py-2 rounded-xl transition">
            Masuk / Daftar
          </button>
        </Link>
      </div>
    </nav>
  );
}*/
